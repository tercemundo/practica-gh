name: Json parse test

on:
  workflow_dispatch:
    inputs:
      parameters_json:
        type: string
        required: false

jobs:
  build_and_deploy:
    name: Json parse test
    runs-on: ubuntu-latest
    steps:
      - name: Parse json input
        run: echo "Hola Mundo"
